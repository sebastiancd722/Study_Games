# Used by Vercel Serverless Functions (and by `vercel dev` locally)
GEMINI_API_KEY=your_key_here

# Optional: restrict CORS for the /api/ai endpoint (comma-separated)
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
